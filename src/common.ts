export enum CarQuality {
  Uncommon = 'uncommon',
  Rare = 'rare',
  Epic = 'epic',
  Legendary = 'legendary',
}

export enum GameServer {
  AL = 'AL',
  GL = 'GL',
}

export type CarClass = 'D' | 'C' | 'B' | 'A' | 'S' | 'R'

export enum WorldLeague {
  Bronze,
  Silver,
  Gold,
  Platinum,
  Legend,
  Diamond,
}
