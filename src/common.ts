export enum Quality {
  Uncommon = 'uncommon',
  Rare = 'rare',
  Epic = 'epic',
  Legendary = 'legendary',
}

export enum GameServer {
  AL = 'AL',
  GL = 'GL',
}

export type CarClass = 'D' | 'C' | 'B' | 'A' | 'S' | 'R'
